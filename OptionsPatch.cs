using HarmonyLib;
using XRL.UI;

namespace ASCIIMod.HarmonyPatches
{
    /*
    [HarmonyPatch(typeof(Options), nameof(Options.SetOption), new[] { typeof(string), typeof(string) })]
    public static class OptionChangedPatch
    {
        static void Postfix(string ID, string Value)
        {
            if (ID == "OptionRealHomosapienNoDmgFloatText")
            {
                ASCIIMod.CustomOPtions.ASCIIOptions.Refresh();
            }
        }
    }
    */
    
}
